// join(): Joins all elements of an array into a string using a specified separator.

let myArray = ['apple', 'banana', 'orange', 'grape'];

// Convert the array into a string, with each element separated by a comma and a space
let myString = myArray.join(', ');

console.log(myString); // Output: "apple, banana, orange, grape"
